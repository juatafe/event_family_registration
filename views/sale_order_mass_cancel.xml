<odoo>
    <!-- Acció massiva per a vista tree -->
    <record id="action_cancel_selected_orders" model="ir.actions.server">
        <field name="name">Cancel·lar Comandes Seleccionades</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="state">code</field>
        <field name="code">records.action_cancel_massive()</field>
        <field name="binding_view_types">list</field>
    </record>
</odoo>
